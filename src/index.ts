import nestedFors from './nestedFors';
import normal from './myPreferredNormal';
import recursive from './recursive';
import generator from './generator';

console.log(nestedFors.calc(100));
console.log(normal(100));
console.log(recursive(100));
console.log(generator(100));
